export const group = '@easythings/easy-view';

## mount(node: JSXElement, container: HTMLElement): () => void

> 用于把组件渲染到页面上 只有页面里可以用

**node:** 类似 `<Main />` 这样的一个标签\
**container:** 一个可以挂载的节点\
**返回值:** 卸载函数

```jsx
const destroy = mount(<Main />, document.body); // 挂载到body
destroy(); // 卸载
```
