export const group = '@easythings/easy-view/jsx-runtime';

## Fragment

> 片段组件

这个组件只会返回它的 children

```jsx
function Main() {
  return (
    <>
      {/* 这里的空标签对 <></> 就是Fragment */}
      hello world
    </>
  );
}
```
