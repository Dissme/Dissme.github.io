export const group = '开始';

## 介绍

\
easy-view 是一个很简单的 MVP 框架。它的 jsx-runtime 可以运行在 worker、node、deno 等 js 容器中，因此开发者可以很简单的实现远程推送组件、微前端、PWA 等。它的渲染器和 jsx-runtime 通过类似 rpc 的通讯方式交互，虽然损耗了一些性能，但是后续有跨平台需求时只需要在宿主平台提供相同的通讯和渲染逻辑，而不需要重写业务代码。
